# Guest Manager Streamlit App - Environment Variables

# The 64-character hex secret used to authenticate with the guest API.
# Generate with: openssl rand -hex 64
GUEST_API_SECRET=

# Base URL of the running Firefly Next.js app (no trailing slash).
APP_BASE_URL=http://localhost:5000
